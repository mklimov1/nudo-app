[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA: любой путь отдаём index.html, чтобы работали прямые ссылки
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
